현재 프로젝트의 `index.html`에 HTML, CSS, JavaScript, 이벤트 처리, 데이터 처리, Supabase/API 호출, 인증, 권한, UI 렌더링 등의 코드가 지나치게 많이 들어가 있습니다.

현재 프로젝트를 **기존 기능을 유지하면서 구조적으로 모듈화하고 리팩토링**해주세요.

단순히 파일을 여러 개로 나누는 것이 목적이 아닙니다.

앞으로 AI를 이용해 지속적으로 기능을 추가하고 수정해도 코드가 복잡해지지 않는 **유지보수 가능한 구조**로 변경하는 것이 최종 목적입니다.

---

# 1. 최우선 원칙

다음 원칙을 반드시 지켜주세요.

### 기존 기능 100% 보존

- 현재 작동하는 기능을 삭제하지 마세요.
- 기존 UI/UX를 임의로 변경하지 마세요.
- 기존 데이터 구조를 임의로 변경하지 마세요.
- 기존 Supabase 테이블 구조를 임의로 변경하지 마세요.
- 기존 로그인/인증 방식을 임의로 변경하지 마세요.
- 기존 권한 체계를 임의로 변경하지 마세요.
- 기존 CRUD 기능을 유지하세요.
- 기존 이벤트 동작을 유지하세요.
- 기존 URL 및 페이지 이동 구조를 유지하세요.
- 기존 사용자 데이터가 손실되지 않도록 하세요.

리팩토링의 목적은 **기능 변경이 아니라 코드 구조 개선**입니다.

---

# 2. 작업 전에 반드시 분석부터

바로 코드를 수정하지 마세요.

먼저 현재 프로젝트 전체를 분석해주세요.

분석할 내용:

1. 현재 파일 구조
2. index.html의 전체 역할
3. JavaScript 코드 영역
4. CSS 코드 영역
5. 페이지/화면 구조
6. 이벤트 처리 구조
7. API 호출 구조
8. Supabase 연결 구조
9. 인증 구조
10. 사용자 권한 구조
11. 데이터 CRUD 구조
12. 전역 변수
13. 중복 함수
14. 중복 UI 코드
15. 중복 API 코드
16. 서로 강하게 결합되어 있는 코드
17. 순환 참조 가능성이 있는 부분
18. 다른 기능에 영향을 줄 가능성이 높은 코드

**분석이 끝나기 전에는 코드를 수정하지 마세요.**

먼저 다음 내용을 보여주세요.

- 현재 구조
- 발견된 문제
- 새로운 구조
- 파일별 역할
- 기존 코드가 어느 파일로 이동할지
- 예상되는 위험 요소
- 리팩토링 순서

---

# 3. 목표 구조

현재 프로젝트에 가장 적합한 구조를 분석한 뒤 아래와 유사한 구조로 정리해주세요.

```text
/
├── index.html
│
├── css/
│   ├── base.css
│   ├── layout.css
│   ├── components.css
│   └── pages/
│       ├── dashboard.css
│       ├── users.css
│       └── suppliers.css
│
├── js/
│   ├── app.js
│   ├── config.js
│   ├── router.js
│   ├── state.js
│   │
│   ├── api/
│   │   ├── supabase.js
│   │   └── apiClient.js
│   │
│   ├── auth/
│   │   ├── auth.js
│   │   ├── session.js
│   │   └── permissions.js
│   │
│   ├── services/
│   │   ├── userService.js
│   │   ├── supplierService.js
│   │   └── dataService.js
│   │
│   ├── pages/
│   │   ├── dashboard.js
│   │   ├── users.js
│   │   ├── suppliers.js
│   │   └── settings.js
│   │
│   ├── components/
│   │   ├── header.js
│   │   ├── sidebar.js
│   │   ├── modal.js
│   │   ├── table.js
│   │   ├── form.js
│   │   └── notification.js
│   │
│   └── utils/
│       ├── date.js
│       ├── format.js
│       ├── validation.js
│       └── helpers.js
│
└── assets/
    ├── images/
    └── icons/
```

단, 현재 프로젝트의 규모와 실제 기능을 분석한 뒤 **불필요한 폴더는 만들지 마세요.**

파일을 나누기 위한 파일 분할은 하지 말고 **각 파일의 책임이 명확한 경우에만 분리**하세요.

---

# 4. index.html 최소화

최종적으로 `index.html`은 애플리케이션의 기본 뼈대만 담당하도록 만들어주세요.

가능하면 다음과 같은 형태를 목표로 합니다.

```html
<!DOCTYPE html>
<html>
<head>
    ...
</head>

<body>
    <div id="app"></div>

    <script type="module" src="./js/app.js"></script>
</body>
</html>
```

index.html 내부에 다음 코드를 장기간 유지하지 마세요.

- 대규모 JavaScript
- 대규모 CSS
- API 호출
- Supabase 로직
- 로그인 처리
- 권한 처리
- 데이터 CRUD
- 복잡한 이벤트 처리
- HTML을 반복적으로 생성하는 대규모 템플릿

---

# 5. 가장 중요한 구조

프로젝트를 다음과 같이 분리해주세요.

```text
UI
 ↓
Page / Component
 ↓
Service
 ↓
API / Supabase
 ↓
Database
```

각 계층의 역할을 섞지 마세요.

### UI / Component

화면을 보여주는 역할만 담당합니다.

### Page

페이지의 동작과 화면 구성을 관리합니다.

### Service

실제 업무 로직을 담당합니다.

예:

```text
공급자 등록
공급자 수정
공급자 삭제
공급자 조회
```

### API / Supabase

데이터베이스와 통신하는 역할만 담당합니다.

### Database

실제 데이터를 저장합니다.

---

# 6. Supabase 관련 코드 분리

Supabase를 사용하는 경우 특히 중요합니다.

다음 코드를 페이지 파일에 직접 작성하지 마세요.

```javascript
supabase.from(...)
supabase.auth...
```

가능하면 다음과 같이 분리하세요.

```text
pages/
    suppliers.js

services/
    supplierService.js

api/
    supabase.js
```

예를 들어:

```text
페이지
↓
supplierService
↓
supabase
↓
Database
```

페이지에서 직접 Database를 조작하지 않는 구조를 목표로 합니다.

---

# 7. 인증과 권한을 반드시 분리

로그인 기능이 있다면 다음을 서로 분리해주세요.

```text
인증
권한
사용자 데이터
화면
```

예:

```text
auth/
├── auth.js
├── session.js
└── permissions.js
```

다음 기능을 명확하게 분리하세요.

- 로그인
- 로그아웃
- 세션 확인
- 현재 사용자 확인
- 사용자 역할 확인
- 관리자 여부 확인
- 공급자별 접근 권한 확인
- 페이지 접근 권한 확인
- 데이터 접근 권한 확인

특히 **UI에서 버튼을 숨기는 것만으로 보안을 구현하지 마세요.**

실제 데이터 접근 권한은 Supabase의 RLS 등 데이터베이스 권한 구조와 함께 검토해야 합니다.

---

# 8. 전역 변수 최소화

현재 코드에서 다음과 같은 전역 변수가 있다면 분석해주세요.

```javascript
let currentUser;
let currentSupplier;
let selectedData;
let currentPage;
let data;
```

필요한 경우 중앙 상태 관리 구조를 만들되, 무조건 복잡한 상태관리 라이브러리를 추가하지 마세요.

작은 프로젝트라면 간단한 `state.js`로 관리하세요.

---

# 9. 중복 코드 제거

다음과 같은 코드가 여러 곳에 있다면 공통 함수로 분리하세요.

- 날짜 변환
- 숫자 포맷
- 금액 포맷
- 권한 확인
- 알림창
- 모달
- 테이블 생성
- 로딩 표시
- 에러 처리
- 입력값 검증
- API 오류 처리

단, 단순히 비슷하게 생겼다는 이유로 억지로 하나의 함수로 합치지 마세요.

**실제로 공통 기능인 경우에만 분리하세요.**

---

# 10. 에러 처리 표준화

각 파일마다 제각각인 방식으로 에러를 처리하지 말고 공통된 방식을 만들어주세요.

예:

```text
API 오류
↓
Service
↓
공통 Error 처리
↓
사용자에게 Notification
```

개발자에게 필요한 상세 오류와 사용자에게 보여줄 메시지를 구분하세요.

---

# 11. 로딩 상태 관리

데이터를 불러오는 모든 주요 기능에 대해 다음 상태를 고려하세요.

```text
Loading
Success
Empty
Error
```

예:

```text
데이터 불러오는 중...
데이터 없음
데이터 불러오기 실패
데이터 정상 표시
```

각 페이지에서 제각각 구현하지 말고 공통 UI를 활용할 수 있도록 구성하세요.

---

# 12. 이벤트 처리 구조 개선

HTML에 이벤트를 지나치게 직접 작성하지 말고 JavaScript에서 관리하세요.

가능하면:

```html
onclick="..."
```

형태를 무분별하게 사용하지 마세요.

대신 JavaScript에서 이벤트를 연결하고 관리하세요.

이벤트가 많아질 경우 이벤트 위임(Event Delegation)도 적절하게 활용하세요.

---

# 13. HTML 생성 코드 관리

JavaScript 내부에 거대한 HTML 문자열이 반복되는 경우 정리해주세요.

예:

```javascript
element.innerHTML = `
    ... 수백 줄 ...
`;
```

이런 코드가 여러 곳에 있다면 Component 또는 Template 단위로 분리하세요.

단, 작은 HTML까지 무조건 파일로 분리하지는 마세요.

---

# 14. 보안 관련 확인

리팩토링 과정에서 다음 항목을 반드시 점검하세요.

- Supabase anon key 사용 구조
- service role key가 프론트엔드에 노출되어 있지 않은지
- 사용자 권한 우회 가능성
- 관리자 권한 검증
- 공급자별 데이터 접근 제한
- RLS 적용 여부
- 민감한 데이터가 브라우저에 불필요하게 노출되는지
- localStorage에 민감한 정보를 저장하고 있지 않은지
- 사용자 입력값 검증
- XSS 가능성이 있는 innerHTML 사용

**보안 문제가 발견되면 단순히 숨기지 말고 문제의 원인과 수정 방법을 함께 알려주세요.**

---

# 15. AI가 앞으로 코딩할 때 지켜야 할 규칙

앞으로 새로운 기능을 추가할 때 기존 `index.html`에 코드를 계속 추가하지 마세요.

새 기능은 반드시 기능에 맞는 모듈을 생성하거나 기존 모듈을 수정하세요.

예:

```text
새로운 공급자 기능
↓
pages/suppliers.js
services/supplierService.js
components/supplierTable.js
```

새로운 기능이 기존 기능과 완전히 독립적이라면 기존 파일에 억지로 코드를 추가하지 마세요.

---

# 16. 파일 하나가 너무 커지지 않도록 관리

특정 파일이 지나치게 커지는 경우 다시 모듈화할 수 있도록 기준을 정하세요.

예를 들어 하나의 JS 파일이 수백~천 줄 이상으로 계속 증가하거나 서로 다른 책임의 코드가 섞이기 시작하면 해당 파일을 다시 분석하여 분리하세요.

하지만 **줄 수만 보고 무조건 파일을 분리하지 마세요.**

핵심은 파일의 책임입니다.

---

# 17. AI의 무분별한 코드 수정 방지

매번 코드를 수정할 때 다음 원칙을 적용하세요.

### 수정 전

```text
현재 기능
↓
관련 파일 확인
↓
의존성 확인
↓
수정 범위 결정
```

### 수정 후

```text
수정된 기능 테스트
↓
관련 기능 테스트
↓
콘솔 오류 확인
↓
API 오류 확인
↓
권한 오류 확인
```

**한 기능을 수정하면서 관련 없는 파일을 대규모로 변경하지 마세요.**

---

# 18. 리팩토링 단계

다음 순서로 진행하세요.

### Phase 1

현재 프로젝트 분석

### Phase 2

파일 구조 설계

### Phase 3

CSS 분리

### Phase 4

API / Supabase 분리

### Phase 5

Authentication / Authorization 분리

### Phase 6

Service 계층 분리

### Phase 7

Page 분리

### Phase 8

Component 분리

### Phase 9

Utility 분리

### Phase 10

index.html 최소화

### Phase 11

import/export 관계 정리

### Phase 12

기존 기능 전체 테스트

---

# 19. 매우 중요한 안전 규칙

리팩토링 도중 한 번에 모든 파일을 변경하지 마세요.

가능하면 단계별로 작업하고 각 단계가 정상적으로 동작하는지 확인하세요.

각 단계마다 다음을 확인하세요.

```text
[ ] 페이지 정상 로딩
[ ] 로그인 정상 작동
[ ] 로그아웃 정상 작동
[ ] 데이터 조회
[ ] 데이터 등록
[ ] 데이터 수정
[ ] 데이터 삭제
[ ] 권한 확인
[ ] 관리자 기능
[ ] 일반 사용자 기능
[ ] 공급자별 데이터 접근
[ ] 모바일 화면
[ ] 콘솔 오류
[ ] Supabase 오류
```

기존 기능이 깨지면 다음 단계로 넘어가지 말고 먼저 수정하세요.

---

# 20. 최종 목표

최종적으로 다음과 같은 구조를 만들어주세요.

```text
index.html
    ↓
app.js
    ↓
Page
    ↓
Component
    ↓
Service
    ↓
API / Supabase
    ↓
Database
```

그리고 새로운 기능이 추가될 때:

```text
기존 index.html 수정
        ↓
        X

새로운 Module 추가
        ↓
        O
```

가 되도록 만들어주세요.

---

# 21. 최종 결과 보고

작업이 끝난 후 반드시 다음 내용을 보고해주세요.

1. 변경된 파일 목록
2. 새로 생성된 파일
3. 삭제된 파일
4. 각 파일의 역할
5. index.html이 얼마나 단순해졌는지
6. Supabase 구조
7. 인증 구조
8. 권한 구조
9. Service 구조
10. Component 구조
11. 발견된 보안 문제
12. 해결한 문제
13. 아직 남아있는 개선 사항
14. 테스트한 기능
15. 테스트하지 못한 기능

그리고 가장 중요하게,

**기존 기능이 실제로 유지되었는지 확인한 후 완료라고 보고해주세요.**

단순히 "리팩토링 완료"라고 말하지 말고 실제로 어떤 기능을 확인했는지 명확하게 보고해주세요.

---

## 작업 시작

지금은 바로 코드를 수정하지 말고,

**① 현재 프로젝트 구조 분석 → ② 문제점 분석 → ③ 새로운 구조 제안 → ④ 파일별 이동 계획**

까지만 먼저 진행해주세요.

제가 계획을 확인한 후 실제 리팩토링을 진행합니다.